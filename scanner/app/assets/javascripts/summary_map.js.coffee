$ ->
	initialize_summary_map()

initialize_summary_map = ->
	if $("#summary_canvas").length
		#AJAX request.
		#Set mapOptions
		#Create Map opject
		#Iterate through locations
		#Create Markers
	else
		return
